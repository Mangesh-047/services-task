import { ConvertDataPipe } from './convert-data.pipe';

describe('ConvertDataPipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertDataPipe();
    expect(pipe).toBeTruthy();
  });
});
